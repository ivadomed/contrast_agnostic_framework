defaults:
  - data: brats
  - model: defaults
  - training: defaults
  - logging: wandb
  - _self_

task: generator
seed: 42
version: v5

hydra:
  job:
    chdir: false
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
