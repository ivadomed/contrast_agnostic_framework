# open-ms — FLAIR-trained, auglab-anchored causal-ablation ladder
# (mechanism decomposition of PALETTE's advantage over SynthSeg-EM: K-means
# clustering vs. Voronoi sub-parcellation vs. real-texture fill). Full AugLab
# intensity stack held fixed on every rung.
# Split out of the old combined open-ms_flair_02_ablation.yaml — see that
# dataset's baseline-anchored counterpart: open-ms_flair_02_ablation_baseline.yaml.
# Same eval_all.csv sources as 01_results.yaml (06_01). Consumed by 06_10
# (aggregate_from_config.py) and 06_11 (significance_from_config.py, same
# config, pass --ref explicitly — see that script's header for the
# substring-ambiguity gotcha).
title: "open-ms — FLAIR-trained, auglab-anchored causal-ablation ladder (PALETTE mechanism decomposition)"
metrics_dir: "${METRICS_ROOT}/open_ms_model/flair"
output_prefix: "02_ablation_auglab"
in_domain_contrast: flair
column_order: [flair, t2w, t1w]

runs:
  - open-ms_flair_auglab_default_20260706_061243
  - open-ms_flair_auglab_kmeans_label_remap_train025_val100_20260707_065414
  - open-ms_flair_auglab_kmeans_label_remap_voronoi_train025_val100_20260707_065409
  - open-ms_flair_auglabAug_v26_6_2_train025_val100_20260706_061243
